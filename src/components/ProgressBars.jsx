import React from 'react'
import ProgressBar from 'react-bootstrap/ProgressBar';

// curriculum vitae
export const HtmlCV = () => {
  const htmlCV = '80%';
  return <ProgressBar now={htmlCV} label={`${htmlCV}`} />;
}

export const CssCV = () => {
  const cssCV = '70%';
  return <ProgressBar now={cssCV} label={`${cssCV}`} />;
}

export const JsCV = () => {
  const jsCV = '55%';
  return <ProgressBar now={jsCV} label={`${jsCV}`} />;
}

export const ReactJsCV = () => {
  const reactCV = '60%';
  return <ProgressBar now={reactCV} label={`${reactCV}`} />;
}

export const PhotoshopCV = () => {
  const photoshopCV = '65%';
  return <ProgressBar now={photoshopCV} label={`${photoshopCV}`} />;
}

export const EnglishCV = () => {
  const englishCV = 'Independent User';
  return <ProgressBar now={englishCV} label={`${englishCV}`} />;
}

export const PortugueseCV = () => {
  const portugueseCV = 'Proficient User';
  return <ProgressBar now={portugueseCV} label={`${portugueseCV}`} />;
}

export const SpanishCV = () => {
  const spanishCV = 'Basic User';
  return <ProgressBar now={spanishCV} label={`${spanishCV}`} />;
}

// services
export const HtmlS = () => {
  const htmlS = '80%';
  return <ProgressBar now={htmlS} label={`${htmlS}`} />;
}

export const CssS = () => {
  const cssS = '70%';
  return <ProgressBar now={cssS} label={`${cssS}`} />;
}

export const BootstrapS = () => {
  const bootstrapS = '30%';
  return <ProgressBar now={bootstrapS} label={`${bootstrapS}`} />;
}

export const JQueryS = () => {
  const JQueryS = '25%';
  return <ProgressBar now={JQueryS} label={`${JQueryS}`} />;
}

export const JsS = () => {
  const jsS = '55%';
  return <ProgressBar now={jsS} label={`${jsS}`} />;
}

export const ReactJsS = () => {
  const reactjsS = '60%';
  return <ProgressBar now={reactjsS} label={`${reactjsS}`} />;
}

export const NodejsS = () => {
  const nodejsS = '20%';
  return <ProgressBar now={nodejsS} label={`${nodejsS}`} />;
}

export const NetlifyS = () => {
  const netlifyS = '75%';
  return <ProgressBar now={netlifyS} label={`${netlifyS}`} />;
}

export const ChatgptS = () => {
  const chatgptS = '0%';
  return <ProgressBar now={chatgptS} label={`${chatgptS}`} />;
}

export const VscS = () => {
  const vscS = '80%';
  return <ProgressBar now={vscS} label={`${vscS}`} />;
}

export const GithubS = () => {
  const githubS = '70%';
  return <ProgressBar now={githubS} label={`${githubS}`} />;
}

export const VslsS = () => {
  const vslsS = '80%';
  return <ProgressBar now={vslsS} label={`${vslsS}`} />;
}